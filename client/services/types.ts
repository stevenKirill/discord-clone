export interface IUser {
  id: string,
  userId: string,
  name: string,
  imageUrl: string,
  email: string,
  createdAt: string,
  updatedAt: string,
}

export interface ICreateServer {
  name: string;
  imageUrl: string;
}
